name: Pull Request to Main
on:
    pull_request:
        types: [opened, reopened, synchronize]
        branches: main

  
jobs:
    my-job:
        runs-on: ubuntu-latest
        steps:
        - name: my-step
          run: echo "Hello World!"